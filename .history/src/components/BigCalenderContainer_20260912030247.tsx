import prisma from "@/lib/prisma";

const BigCalenderContainer  = async ({type, id }:{type:"teacher" |"student" , id: string | number}) => {
  return (


    const data = await prisma.lessons.findM

    <div className=''>

    </div>
  )
}

export default BigCalenderContainer